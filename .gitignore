__pycache__/
*.pyc
.venv/
.DS_Store

# benchmark run artifacts — regenerated by run_battery.py / score_*.py, not source
benchmark/results/*
!benchmark/results/.gitkeep

# packet-builder run artifacts (smoke tests etc.) — same class as benchmark/results
benchmark/packets/
